# 用法：复制为 .env 并填写（.env 已被 Git 忽略）
#   cp .env.example .env && nano .env
# 当前 shell 加载：在项目根目录执行
#   set -a && source .env && set +a
# scripts/run_nim.sh 会自动 source 项目根目录的 .env

NGC_API_KEY=

# 可选
# NIM_TAG=2.0.0
# NIM_PORT=8000
# NIM_IMAGE=
# SKIP_DOCKER_LOGIN=0
